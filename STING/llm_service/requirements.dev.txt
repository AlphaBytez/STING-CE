# Lightweight development requirements - faster build times
# Use this for faster development builds: COPY requirements.dev.txt instead of requirements.common.txt

# Server and API (essential)
fastapi>=0.95.0
uvicorn>=0.22.0
pydantic>=2.0.0

# Utilities (essential)
requests>=2.28.0
python-dotenv>=1.0.0
httpx>=0.24.0

# Minimal ML (comment out for fastest builds)
# transformers>=4.30.0
# torch>=2.0.0

# Essential chatbot dependencies
llama-index>=0.10.0
langchain>=0.1.0